

initialState = {
    id: 1,
    Title: "Greg and the black Pirates",
    Category: "Story"
}
const bookReducer = (state = initialState, action) {
    switch (action.type) {
        case ADD_BOOK:
            return 
                ...state
            }
        default:
            return state;
    }
}